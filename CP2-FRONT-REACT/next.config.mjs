// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['apod.nasa.gov', 'images-assets.nasa.gov'], 
    },
  };
  
  export default nextConfig;
  